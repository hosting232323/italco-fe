lint_frontend:
  stage: lint
  image: node:22
  interruptible: true
  allow_failure: true
  tags:
    - rattata-lint
  rules:
    - if: '$CI_COMMIT_BRANCH != "main"'
  script:
    - npm ci
    - npx eslint "src/**/*.{js,vue}"
